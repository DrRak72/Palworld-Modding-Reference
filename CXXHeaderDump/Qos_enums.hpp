enum class EQosCompletionResult {
    Invalid = 0,
    Success = 1,
    Failure = 2,
    Canceled = 3,
    EQosCompletionResult_MAX = 4,
};

enum class EQosDatacenterResult {
    Invalid = 0,
    Success = 1,
    Incomplete = 2,
    EQosDatacenterResult_MAX = 3,
};

enum class EQosResponseType {
    NoResponse = 0,
    Success = 1,
    Failure = 2,
    EQosResponseType_MAX = 3,
};

